const http = require('http');
const fs = require('fs');

/**
 * @param {import('yargs').Arguments} argv
 */
exports.remoteList = async function(argv) {
}

/**
 * @param {import('yargs').Arguments} argv
 */
exports.remoteGet = async function(argv) {
}
